package ru.megains.common.managers

object BlockManager {

    //    def initBlocks(): Unit ={
    //        GameRegister.getBlocks.filter(_.format == BlockFormat.MACRO).foreach((block)=>{
    //            block.asInstanceOf[MacroBlock].blockSize = BlockAxisAlignedBB.round
    //        })
    //    }
}
